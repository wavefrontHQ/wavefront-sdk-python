name: Set Global Environment Variables

on:
  workflow_call:
    outputs:
      package_name: 'wavefront_sdk'
      requirements: 'Deprecated requests tdigest'

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
    - run: echo "This job sets environment variables for others."
